______________________
Форматирование строк
______________________

- конкатенация (не рекомендуется)

- %переменная/выражение

- print('somestring {}'.format(переменная/выражение что будет вставляться в {})) 
		    _____________________________________________

- print(f'Somestring {переменная/выражение} {переменная/выражение} ...')  (рекомендуется)